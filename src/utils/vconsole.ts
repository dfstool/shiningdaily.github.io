// import VConsole from 'vconsole';

// const vConsole = new VConsole();
// vConsole.setSwitchPosition(0, 0);


// 定义空的vConsole变量，只是为了屏蔽vConsole的功能，方便报错时查看代码错误位置
let vConsole = {
  hide: () => {},
  showSwitch: () => {},
  hideSwitch: () => {},
}

export default vConsole;